# Alpha
OneWoW_AltTracker2/

.releases/
.gitignore
.git/
.cache/
__pycache__
CURSEFORGE.md
.wow_docs/manifest-review.txt

# Ricky's Cursor rules — local only, never commit or push
# (AccountSync, Triggers, Gitmode, Audit, BugReport, ChangeLog, Dev-AddOn-Links, Update-Time, etc.)
# `git add` will refuse these unless you force; keep them out of the shared repo.
.cursor/rules/1W-Ricky-*.mdc
.cursor/rules/Git-Commit-Rules.mdc

# Claude Code local permissions (personal)
.claude/settings.local.json

# Local secrets (never commit)
.env
.env.*

# Legacy local Discord send scripts / payloads (obsolete — server bot only; ignore if leftover)
scripts/Load-DiscordEnv.ps1
scripts/Send-ChangelogToDiscord.ps1
scripts/Update-DevToolChangelogOnServer.ps1
.discord-changelog-send.json*

# Local Documentation - Never Commit
guiplanner.md
apiplanner.md
dbplanner.md


# Local Developer Tools - Never Commit
ONEWOW_SUITE.code-workspace
OneWoW_CatalogData_Quests/Tools/

# Audit tools: ignore everything EXCEPT the two published report files
bin/audit-tools/*
!bin/audit-tools/reports/
bin/audit-tools/reports/*
!bin/audit-tools/reports/summary.json
!bin/audit-tools/reports/AUDIT_SUMMARY.md

# Addon-local diagnostic tools belong outside shipped load units.
OneWoW*/[Tt]ools/
